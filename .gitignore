*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Supabase API Keys (sensitive)
# Never commit API keys to version control
# Note: local.properties is already excluded on line 3 and 15 above
# Explicitly exclude all local.properties files at any depth
**/local.properties

# Exclude specific .properties files only (avoid broad pattern to prevent accidental exclusions)
# Using explicit patterns instead of *.properties to ensure gradle.properties is not excluded
# gradle.properties should be tracked in version control
